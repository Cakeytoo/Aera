import React from "react";
import ReactDOM from "react-dom/client";
import App from "../Pages/Home"; // adjust if your entry is different

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
